# Скопируйте в .env и подставьте свои значения.

# URL бэкенда (без /api — он добавляется в lib/api.js)
VITE_API_URL=http://localhost:3000

# Только для локальной разработки через туннель (ngrok и т.п.).
# Список хостов через запятую; пусто — туннель не используется.
VITE_DEV_ALLOWED_HOSTS=
